{
  "family": "cartforge-task",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::256097482448:role/ecsTaskExecutionRole",
  "containerDefinitions": [
    {
      "name": "cartforge",
      "image": "256097482448.dkr.ecr.us-east-1.amazonaws.com/cartforge:latest",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 80,
          "protocol": "tcp"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/cartforge",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "cartforge"
        }
      }
    }
  ]
}
